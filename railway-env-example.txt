# Railway Environment Variables
# Copy these to your Railway project settings

# Django Settings
DJANGO_SETTINGS_MODULE=e2i_api.settings
DJANGO_DEBUG=False
DJANGO_SECRET_KEY=your-secret-key-here-change-in-production
DJANGO_ALLOWED_HOSTS=your-app.railway.app,localhost

# Database (Railway PostgreSQL addon will provide these automatically)
# DATABASE_URL is automatically provided by Railway PostgreSQL addon
# No need to set individual database variables - DATABASE_URL is sufficient

# Optional: Override individual database settings if needed
# DJANGO_DB_ENGINE=django.db.backends.postgresql
# DJANGO_DB_NAME=railway
# DJANGO_DB_USER=postgres
# DJANGO_DB_PASSWORD=your-db-password
# DJANGO_DB_HOST=your-db-host.railway.app
# DJANGO_DB_PORT=5432

# Optional: External Services (if not using Railway addons)
MINIO_ENDPOINT=your-minio-endpoint
MINIO_ACCESS_KEY=your-minio-access-key
MINIO_SECRET_KEY=your-minio-secret-key
MINIO_BUCKET=uploads

CLICKHOUSE_HOST=your-clickhouse-host
CLICKHOUSE_PORT=9000
CLICKHOUSE_DB=default
CLICKHOUSE_USER=user
CLICKHOUSE_PASSWORD=password

REDIS_URL=your-redis-url

# Orchestrator (if using external service)
ORCHESTRATOR_URL=http://your-orchestrator-url

# Tokenization (if using external service)
TOKENIZATION_ENDPOINT=http://your-tokenization-url
TOKENIZATION_API_KEY=your-tokenization-api-key
